# Ferramentas

Procuradoria Geral do Estado MT: Dívida Ativa expõe 1 ferramenta (todas somente leitura).

### 1. `pge_mt_divida_ativa_consultar`
**Input**: `cda` (opcional), `cnpj` (opcional), `cpf` (opcional), `renavam` (opcional), `chassi` (opcional), `pagina` (opcional)

Procuradoria Geral do Estado MT: Dívida Ativa, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Procuradoria Geral do Estado MT: Dívida Ativa
```
